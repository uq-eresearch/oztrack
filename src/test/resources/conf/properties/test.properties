application.version=0.1.0

# Configuration of database
application.persistenceUnitName=test-pu
application.uri.prefix=http://localhost:8080/
application.title=OzTrack
application.email=peggy.newman@uq.edu.au
application.rights=All Rights reserved
application.dataDir=

dataSpaceURL=
dataSpaceUsername=
dataSpacePassword=

serverProxyName=

# Configuration of outgoing email
#
# If the submissionEmailAddress is missing or empty, the feature
# will be disabled (which is the default).
#
# Note that the submission form is not protected, i.e. people can
# use it to send arbitrary data to the address configured here.
application.emailServer=smtp.uq.edu.au
